{% extends 'temp.html.twig' %}


{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{% for stage in stages %}
<div>
    <p><u>Titre :</u> {{stage.Titre}}</p>
    <p><u>Description :</u> {{stage.Mission}}</p>
    <p><u>Email de l'entreprise :</u> {{stage.Email}}</p>
    <p><u>Entreprise proposant le stage :</u> {{entreprises.Nom}}</p>
    <p><u>Formations autorisées pour le stage :</u></p>
    {% for formation in formations %}
        <p>{{formation.NomLong}}({{formation.NomCourt}})</p>
    {% endfor %}
</div>
{% endfor %}
{% endblock %}


