{% extends 'temp.html.twig' %}


{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{% for stage in stages %}
</br>
    <div class="container-fluid bg-grey border border-dark" align=center>
        <p style="font-weight:bolder;font-size:large">{{stage.Titre}}</p>
        <p><u>Description :</u> {{stage.Mission}}</p>
        <p><u>Email de l'entreprise :</u> {{stage.Email}}</p>
        <p><u>Entreprise proposant le stage :</u> <a href="http://127.0.0.1:8000/stage/entreprise/{{entreprises.id}}" >{{entreprises.nom}}</a>
    <p><p>
        <p><u>Formations autorisées pour le stage :</u></p>
        
        {% for formation in formations %}
            {{formation.nomLong}} <a href="http://127.0.0.1:8000/stage/formation/{{formation.id}}" >( {{formation.nomCourt}} )</a>
            <br>
        {% endfor %}

    </div>

{% endfor %}
{% endblock %}


